---
title: Installation
description: Install AI Toolkit and configure peer dependencies
---

import { InstallTabs } from '@/components/install-tabs';

## Install the Package

<InstallTabs packages={["@jamaalbuilds/ai-toolkit"]} />

The core package includes all modules. Peer dependencies are only needed for the modules you use.

## Peer Dependencies

Each module wraps an underlying library. Install only the peer deps for the modules you use:

### AI (generate, stream, structured output)

<InstallTabs packages={["ai", "@ai-sdk/groq"]} />

Or use OpenRouter for access to multiple providers:

<InstallTabs packages={["ai", "@openrouter/ai-sdk-provider"]} />

### Chain (prompt templates, output parsing)

<InstallTabs packages={["@langchain/core", "@langchain/textsplitters"]} />

### Agents (multi-agent orchestration)

<InstallTabs packages={["@langchain/langgraph", "@langchain/core"]} />

### Knowledge (document ingestion, search)

<InstallTabs packages={["@llamaindex/liteparse"]} />

### Database (typed queries, vector search)

<InstallTabs packages={["drizzle-orm", "postgres"]} />

### Monitor (tracing, evaluation)

<InstallTabs packages={["langfuse"]} />

### Workflow (background jobs, cron)

<InstallTabs packages={["inngest"]} />

### MCP (Model Context Protocol servers)

<InstallTabs packages={["@modelcontextprotocol/sdk", "zod"]} />

### Cache (Redis adapter)

<InstallTabs packages={["ioredis"]} />

### Storage (file uploads)

<InstallTabs packages={["@vercel/blob"]} />

## TypeScript Configuration

AI Toolkit is ESM-only. Your `tsconfig.json` should include:

```json title="tsconfig.json"
{
  "compilerOptions": {
    "module": "esnext",
    "moduleResolution": "bundler",
    "target": "es2022"
  }
}
```

## Environment Variables

Configure your AI provider credentials:

```bash title=".env"
# .env
GROQ_API_KEY=gsk_...
# or
OPENROUTER_API_KEY=sk-or-...
# or
OPENAI_API_KEY=sk-...
```

## Verify Installation

```ts title="verify.ts"
import { initToolkit } from '@jamaalbuilds/ai-toolkit/config';

initToolkit();
console.log('AI Toolkit is ready!');
```
